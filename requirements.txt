# Đường tất định (Stage A→G). Chạy CPU thuần, không cần GPU/internet/model.
# Phiên bản ghim đúng bản đã dùng để tạo ra các số đo trong README.
pandas==3.0.5
pyarrow==25.0.1
numpy==2.5.2
scipy==1.18.0
tabulate==0.10.0
